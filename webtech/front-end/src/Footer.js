/** @jsxImportSource @emotion/react */

const styles = {
  footer: {
    padding: "10px",
    backgroundColor: "#ffffffda",
    flexShrink: 0,
    textAlign: "center",
    paddingTop: "10px",
    color: "#23303f",
    fontSize: "14px",
  },
};

export default function Footer() {
  return (
    <footer style={styles.footer}>
      WebTech Project, Hugo BEHERAY Zoé CROUZET
    </footer>
  );
}
